/**
 * React Native Adapter for Firebase Auth SDK
 * 
 * OPTIONAL: Only import this if you're using React Native
 * The core SDK works without React Native!
 */

export { AuthProvider, useAuth } from './AuthContext';
export { AuthWidget } from './AuthWidget';