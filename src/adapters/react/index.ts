
// src/adapters/react/index.ts

/**
 * React Adapter for Firebase Auth SDK
 * 
 * OPTIONAL: Only import this if you're using React
 * The core SDK works without React!
 */

export { AuthProvider, useAuth } from './AuthContext';
export { AuthWidget } from './AuthWidget';